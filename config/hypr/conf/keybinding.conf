source = ~/.config/hypr/conf/keybindings/default.conf

# Un solo lanzador: antes Super+Espacio abría wofi (sin tematizar, aspecto GTK por
# defecto) y Super+Ctrl+Enter abría rofi. Ahora los dos abren el mismo.
bind=SUPER,Space,exec,pkill rofi || rofi -show drun -replace -i
